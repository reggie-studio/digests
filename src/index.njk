---
layout: base.njk
title: Home
---
<div class="text-center py-8 mb-8">
  <h1 class="text-4xl font-bold text-white mb-3">🥷 Reggie Digests</h1>
  <p class="text-slate-400 text-lg">Curated insights on AI, tech, and things worth knowing</p>
</div>

<section class="space-y-4">
  {% for digest in collections.digests %}
  <article class="bg-slate-800 rounded-xl p-6 border border-slate-700 hover:border-brand transition-all hover:-translate-y-0.5">
    <div class="flex items-center gap-3 mb-3 text-sm text-slate-400">
      <time>{{ digest.data.date | shortDate }}</time>
      {% if digest.data.categories %}
      {% for cat in digest.data.categories %}
      <span class="bg-slate-700 text-slate-200 px-3 py-1 rounded-full text-xs font-medium">{{ cat }}</span>
      {% endfor %}
      {% endif %}
    </div>
    <h3 class="text-xl font-semibold mb-2">
      <a href="{{ digest.url | url }}" class="text-white hover:text-brand transition-colors">{{ digest.data.title }}</a>
    </h3>
    {% if digest.data.summary %}
    <p class="text-slate-400">{{ digest.data.summary }}</p>
    {% endif %}
  </article>
  {% else %}
  <p class="text-slate-400 text-center py-12">No digests yet. Check back soon!</p>
  {% endfor %}
</section>
